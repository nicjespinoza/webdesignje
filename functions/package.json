{
    "name": "functions",
    "description": "Firebase Cloud Functions for WebDesignJE Chat API",
    "scripts": {
        "build": "tsc",
        "build:watch": "tsc --watch",
        "serve": "npm run build && firebase emulators:start --only functions",
        "deploy": "firebase deploy --only functions"
    },
    "engines": {
        "node": "20"
    },
    "main": "lib/index.js",
    "dependencies": {
        "@google/generative-ai": "^0.24.0",
        "cors": "^2.8.5",
        "firebase-admin": "^13.4.0",
        "firebase-functions": "^6.3.2"
    },
    "devDependencies": {
        "@types/cors": "^2.8.17",
        "typescript": "^5.7.3"
    },
    "private": true
}