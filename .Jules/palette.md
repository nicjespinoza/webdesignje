## 2024-04-30 - Accessible Tooltips for Icon-only Buttons
**Learning:** Adding `title` along with `aria-label` provides a native browser tooltip on hover for sighted users, complementing screen reader accessibility for icon-only buttons.
**Action:** Always include a `title` attribute matching the `aria-label` value when adding accessibility to icon-only interactive elements in the application.
