## 2026-06-06 - Added Navbar ARIA Attributes
**Learning:** For a11y, ensure mobile menu toggles have `aria-label`, `aria-expanded`, and `aria-controls`.
**Action:** Always include complete set of ARIA attributes on toggle buttons and language selectors in React components.
